{
  "name": "rectg",
  "version": "1.0.0",
  "description": "Render README.md to styled static HTML for deployment.",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js"
  },
  "devDependencies": {
    "marked": "^12.0.2"
  }
}
